/** @type {import('next').NextConfig} */
const nextConfig = {
    // Agar images kahin bahar se aa rahi hain toh unhe allow karna padta hai
    // Filhal hum isse simple rakhenge
    reactStrictMode: true,
  };
  
  export default nextConfig;